# 抽象データ型仕様: {システム名}

## 型定義

### ソート（型の集合）

| ソート | 説明 | フィーチャーによる変化 |
|--------|------|---------------------|
| {型名} | {説明} | {どのフィーチャーが影響するか} |

### 型パラメータ

| パラメータ | 対応フィーチャー | 制約 |
|-----------|----------------|------|
| {パラメータ名} | {フィーチャー名} | {型制約} |

## 操作

### {操作名}

- **シグネチャ**: `{操作名}({パラメータ}: {型}, ...) → {返り値型}`
- **カテゴリ**: コンストラクタ / ミューテータ / オブザーバ / デストラクタ
- **共通/可変**: 共通 or 可変（{条件}）
- **事前条件**: {呼び出し前に満たすべき条件}
- **事後条件**: {呼び出し後に保証される状態}
- **フィーチャーによる変化**:
  - {フィーチャー} = {値} の場合: {事後条件の変化}

## 不変条件

### 共通不変条件（すべての構成で成立）

| # | 不変条件 | 説明 |
|---|---------|------|
| I{N} | {形式的な記述} | {自然言語での説明} |

### 条件付き不変条件（特定のフィーチャー選択時に成立）

| # | 条件 | 不変条件 | 説明 |
|---|------|---------|------|
| CI{N} | {フィーチャー条件} | {形式的な記述} | {自然言語での説明} |

## フィーチャーによるパラメタライズ

| フィーチャー | 型への影響 | 操作への影響 | 不変条件への影響 |
|-------------|-----------|-------------|----------------|
| {フィーチャー名} | {型の変化} | {操作の有無や意味論の変化} | {追加/変更される不変条件} |
